**Cinematic Homepage Motion QA**

- desktop idle: `artifacts/cinematic-home-idle.png`
- desktop running: `artifacts/cinematic-home-running.png`
- desktop complete: `artifacts/cinematic-home-complete.png`
- mobile complete: `artifacts/cinematic-home-mobile.png`
- reduced motion: `artifacts/cinematic-home-reduced-motion.png`
- desktop viewport: `1576x1118`
- mobile viewport: `390x844`

**Verified states**

- Input fields remain visible before the user starts the ritual.
- The cinematic stage begins only after a valid form submission.
- No number is shown before the real prediction response resolves.
- Error and quota-exhausted states do not save fallback results.
- Reduced-motion mode preserves the full workflow without long animation.
- Desktop and mobile have no horizontal overflow or control overlap.

**Capture notes**

- Screenshots were generated by `tests/capture_motion_qa.py` against `http://127.0.0.1:8017/`.
- The Playwright harness fixes `/api/games`, `/api/quota/status`, `/api/predict`, `/api/review/*`, and cloud-record responses so the evidence is independent from mutable SQLite quota state.
- The fixed prediction payload renders `03 08 16 21 27 32 09`, and the script verifies both motion-stage balls and saved result text match it.
- Programmatic checks confirm all five images have nonblank pixel variance and the expected dimensions.
- Visual inspection confirmed sensible desktop/mobile framing, readable steps, no horizontal crop of active content, and no text, control, progress, or number overlap.

---

# 福彩3D 工具箱视觉与可访问性 QA（Iteration 1 发布验收）

Generated by `PYTHONPATH=. ../../.venv/bin/python tests/capture_retention_qa.py` from the
worktree root. The harness serves temporary SQLite fixtures only (120 deterministic 福彩3D
draws), freezes `today=2026-07-13`, disables AI and auto-update (`lifespan="off"`), and the
browser context aborts every request that does not go to the local fixture server. It never
reads or writes `cwl_history/cwl_history.sqlite`.

Each invocation captures the whole journey twice, against independent temporary databases and
Chromium instances, and requires byte-identical SHA-256 output for all four artifacts. Browser
time (`2026-07-13 12:00 Asia/Shanghai`), locale, random sources, colour scheme and scroll
origin are fixed; animation, transition and caret effects are suppressed; captures wait for
network idle, `document.fonts.ready` and image decoding.

The pre-toolbox artifacts (`fc3d-home-save-*.png`, `fc3d-workbench-*.png`,
`fc3d-plan-review-*.png`, `fc3d-stale-state-mobile.png`) are historical evidence of the
简单/专业 workbench that Iteration 1 removed. The capture script no longer regenerates them.

## 1. 截图证据

| Artifact | 页面 / 状态 | Viewport | 实际尺寸 | Fixture |
| --- | --- | --- | --- | --- |
| `artifacts/fc3d-toolbox-home-desktop.png` | 工具箱首页：数据状态、4 组共 8 个工具、本期方案条（工具之后） | 1440x1000 | 1440x1439 | fresh temp DB，最新 `2026193 / 2026-07-12`，本期 `2026194 / 2026-07-13` |
| `artifacts/fc3d-toolbox-home-mobile.png` | 工具箱首页的**首屏本身**（390x844，非整页）：验收移动端首屏标准 | 390x844 | 390x844 | 同上 |
| `artifacts/fc3d-toolbox-trend-desktop.png` | 走势工具，近 30 期（30 行真实开奖，遗漏列为落号前的真实遗漏期数） | 1440x1000 | 1440x2782 | 同上 |
| `artifacts/fc3d-toolbox-reduction-mobile.png` | 缩水选号：和值 6-18、跨度 1-8、组六、奇数 2、百位含 1、十位排除 9，生成候选后（筛后 32 组） | 390x844 | 390x5016 | 同上 |
| `artifacts/fc3d-toolbox-stale-mobile.png` | stale 数据下的缩水选号：条件可编辑、本期生成与保存全部禁用 | 390x844 | 390x2366 | stale temp DB，最新 `2026193 / 2026-07-05`，本期 `2026194 / 2026-07-06` |

两次独立捕获得到的 SHA-256（脚本内比对，必须一致）：

| Artifact | SHA-256 |
| --- | --- |
| `fc3d-toolbox-home-desktop.png` | `4bf2f1cbe15effef15e7ee829917c715fac7d99040f85dbfca946b361a8c814a` |
| `fc3d-toolbox-home-mobile.png` | `c9164fff0d8128495b83c8b853f42e71c720924adca257939e98bbd62243a288` |
| `fc3d-toolbox-trend-desktop.png` | `c2d1f865f9742633647c3bf264afd737c8b98ec623a283c9575d44fcb60c3820` |
| `fc3d-toolbox-reduction-mobile.png` | `23673399e5a30db8b9eb1b12ed745a99aaa9fa9a885914638d98b81ff786b703` |
| `fc3d-toolbox-stale-mobile.png` | `45e0278fc95c894995a5dc5c25a63f19c6933de1b5a83a89f35bf3bf88f62abc` |

自动断言（每张截图捕获前执行）：无页面横向溢出、关键文案不截断、关键控件不重叠、CTA 不被遮挡、
所有可见按钮和链接的触达高度 ≥ 40px、走势表 30 行、走势遗漏列 90 个值非负且不是常数（旧实现恒为 0
会直接失败）、候选列表非空、stale 下 `生成候选` / `保存手动` / `随机一组` / `保存筛选` 全部 disabled，
且 stale 下走势、遗漏、最近开奖、号码查询仍返回真实结果。移动端首屏标准（状态条完整可见 + ≥ 6 个工具
入口在 844px 之上）由 `measure_mobile_first_screen` 断言，不再只是打印。

## 2. 真实视觉对照（逐张看图的结论）

对照 Step 3 的六条要求，看图结论如下。

### 2.1 工具可发现性 — 通过（桌面与移动端首屏，F1 已修复）

`fc3d-toolbox-home-desktop.png`：8 个工具按 走势统计(4) / 号码查询(2) / 选号工具(1) / 开奖记录(1)
分成 4 组，每个磁贴有图标、名称和一句说明，一屏可见，不需要滚动。本期方案条现在位于工具网格之后。

发布标准是“390px 首屏能看到开奖数据状态 + 至少 6 个工具入口”。修复前（旧版截图，脚本输出
`mobile_first_screen`）首屏内工具入口 = **0**，数据状态条底边落在 y=931（首屏 844）：

| 区块 | 修复前高度 / 底边 y | 修复后高度 / 底边 y |
| --- | --- | --- |
| `.site-header` | 202px / 222 | **86px / 100** |
| `#gameTabs` | **290px**（5 个全宽按钮纵向堆叠）/ 540 | **42px**（单行横向滚动）/ 154 |
| 工具箱标题区 | 154px / 753 | 53px / 236 |
| 数据状态条 `#threeDFreshness` | 162px / **931（超出首屏）** | 110px / **356（完整可见）** |
| 本期方案条 `#threeDPlanStrip` | 99px / 1047（工具之前） | 88px / 1062（**移到工具之后**） |
| 第一个工具磁贴顶边 | 1099 | **392** |

修复后实测（`tests/capture_retention_qa.py` 的 `mobile_first_screen`，scrollY=0，fold=844）：
数据状态条完整可见，首屏内**完整露出 7 个工具入口**（走势图、遗漏统计、出次统计、冷热码、号码查询、
号码属性、缩水选号），只有 最近开奖 需要滚动；磁贴最小高度 92px，仍远高于 40px 触达下限。
证据见 `fc3d-toolbox-home-mobile.png`（该图就是 390x844 的首屏本身）。

### 2.2 黑金视觉是否牺牲可读性 — 发现两处不达标，已修复

看图时工具磁贴的说明文字（“按期查看百十个位变化”等）明显偏暗。实测这些文字用的是站点级
`--quiet: #806335`，在工具箱近黑底上只有 **3.72:1**，低于 12px 正文所需的 4.5:1（WCAG AA）。
同色号还用在走势表表头（期号/日期/百位…）、工作区 kicker、`本期目标：--` 一类的 band 说明和
`组态`/`奇数个数` 的 legend 上，一共 5 类，全部不达标。

修复：工具箱内新增 `--three-d-quiet: #a98d59`（6.4:1），替换工具箱内全部 `var(--quiet)`，不改动其他
页面仍在使用的站点级 token。新增可失败的守卫测试
`test_3d_toolbox_secondary_text_meets_wcag_aa_contrast`：它用元素截图取真实背景众数色 + 计算样式
取文字色，按字号判定 AA 阈值，修复前 5 项红、修复后全绿。

其余文字实测：h1 17.5:1、数据状态 16.8:1、工具名 9.6:1、免责说明 9.0:1、表格数据 9.6:1、
未选中窗口标签 6.5:1、选中窗口标签 10.7:1 —— 全部达标。

### 2.3 图标 — 发现严重缺陷，已修复

第一版截图里，8 个工具磁贴的图标几乎看不见，走势工具左上角的返回按钮更是一个**空方块**。
原因：官方 Lucide 文件用 `stroke="currentColor"`，而它们是通过 `<img src>` 引入的；`<img>` 内的
`currentColor` 解析为 SVG 文档自身的初始色 **黑色**，于是黑图标画在近黑底上。返回按钮没有可见文案，
所以它对视力用户等于没有可见标记（读屏用户靠 `aria-label` 反而是好的）。

修复：保持官方 Lucide 资源文件不变，改用 CSS `mask-image` 引入，颜色由 `background-color: var(--gold)`
提供（`.three-d-icon`）。新增守卫测试 `test_3d_toolbox_icons_are_painted_in_a_visible_color`：断言
8 个磁贴图标和返回按钮图标的 mask 指向 `assets/icons/*.svg`，且自身绘制色相对纯黑的对比度 ≥ 3:1
（`<img>` 实现下该断言必然失败，因为元素根本没有绘制色）。重新捕获后，截图里 8 个图标与返回箭头
都是清晰的金色。

### 2.4 首屏没有巨型标题或装饰性留白 — 通过（含一个桌面留白问题）

`福彩3D工具箱` h1 在 1440px 下是 56px 单行，右侧紧跟 `历史方案` 入口，下面直接是免责说明和数据状态条，
没有 hero 插画、没有整屏留白。1440px 首屏内已经能看到数据状态 + 走势统计全组 4 个工具。

但桌面下的工具网格是固定 4 列：号码查询组只有 2 个工具、选号工具组 1 个、开奖记录组 1 个，
于是页面下半部分出现三行大面积空单元格（每行 1/2 到 3/4 宽度是空的）。这是真实可见的装饰性空白，
不影响功能。见 §4 修复项 F2。

### 2.5 工具卡不是卡片嵌套 — 通过

磁贴是一个共享边框的扁平网格（`.three-d-tool-grid` 只画 1px 分隔线），不是“卡片里再放卡片”。
工具工作区是单层面板：面板 → 表格 / 表单 / 列表。缩水选号的候选项是列表行（复选框 + 号码 + 属性），
在面板内只有一层边框，没有出现三层套娃。

### 2.6 最长中文标签在 390px 不截断、不覆盖 — 通过

390px 下逐项看图并断言：`按条件减少候选范围`（工作区 kicker）、`数据待更新（最新数据 2026193 /
2026-07-05），暂不能保存本期方案。`、`原始范围 1000 组 / 筛后候选 32 组 / 显示前 20 组`、
`百位包含 / 百位排除 / 十位包含 / 十位排除 / 个位包含 / 个位排除`、候选属性行
`和值6 / 跨度5 / 组六 / 奇偶2:1 / 012 1:1:1` 全部完整换行显示，没有截断，没有互相覆盖，
页面无横向滚动（`assert_text_not_clipped` / `assert_no_key_overlap` /
`assert_no_page_horizontal_overflow` 均通过）。

### 2.7 数据状态与禁用原因紧邻受影响动作 — 发现一处不达标，已修复

stale 截图第一版：`保存手动` 是一个**金色实心主按钮**（disabled，只靠 opacity 0.56 变暗），它下面的
状态行写的却是 `等待输入。`——真正的 stale 原因写在两屏之外的筛选结果区。用户看到的是“一个看起来
可用的主按钮 + 一句和禁用无关的提示”。

修复：`保存手动 / 随机一组` 被 stale 数据禁用时，`#threeDManualStatus` 直接写出原因和它在等的数据：
`数据待更新（最新数据 2026193 / 2026-07-05），暂不能保存本期方案。`（错误色），并在数据恢复后自动
还原为 `等待输入。`，且不会覆盖保存成功/失败的结果文案。测试
`test_3d_reduction_tool_keeps_conditions_editable_but_blocks_current_issue_when_stale` 已扩展为
断言该文案与数据日期。重新捕获的 `fc3d-toolbox-stale-mobile.png` 里，原因就在两个禁用按钮正下方。

`生成候选` / `保存筛选` 的禁用原因本来就在按钮下方的结果区（`筛选结果`），并带最新数据期号与日期；
顶部数据状态条同时显示 `数据待更新` + 最新期号/日期。这三处通过。

## 3. 可访问性检查结果

以 Playwright 实测（1440x1000 桌面 + 390x844 移动 + 720x422 = 200% 缩放等效视口）：

| 项目 | 结果 |
| --- | --- |
| 键盘 Tab 顺序（工具箱首页） | 通过。品牌 → 页面导航 → 彩种 → 数据状态动作（重试 / 历史方案）→ 方案动作 → 8 个工具磁贴，顺序即文档顺序，无焦点陷阱。 |
| 打开工具后的焦点 | **修复后通过**。此前点击磁贴后磁贴被隐藏，焦点掉回 document.body。现在 `openTool` 把焦点移入 `#threeDToolWorkspace`（`tabindex="-1"`），返回工具箱时焦点回到刚才那个磁贴。守卫测试 `test_3d_tool_open_moves_focus_into_the_workspace_and_back_restores_the_tile`。 |
| 工具内 Tab 顺序 | 通过。工作区 → 返回按钮 → 近30/60/120期 → 走势表格滚动区（`tabindex=0`，`aria-label="走势图表格"`，键盘可滚）。 |
| 返回按钮可访问名称 | 通过。图标按钮 `aria-label="返回工具箱"`，图标 `aria-hidden`，读屏可读；修复图标颜色后视力用户也看得见。 |
| 窗口标签 `aria-pressed` | 通过。近30期 `true`、近60期 `false`、近120期 `false`，切换后同步。 |
| 错误 `aria-live` | 通过。走势刷新失败时，`加载失败，请重试。` 落在 `#threeDToolWorkspace`（`aria-live="polite"`）内；查询、缩水、保存的反馈元素均带 `role="status"`。 |
| `prefers-reduced-motion` | 通过。reduce 下工具箱内控件的 `transition-duration` 为 `0.00001s`。 |
| 文本对比度 | **修复后通过**。见 §2.2：5 类 12–13px 次要文字原为 3.6–3.7:1，现为 6.4:1；其余 6.5–17.5:1。 |
| 触达目标 ≥ 40px | **修复后通过**。`历史方案` 头部入口原 34px；数据状态条内的 `历史方案` 链接 21px；方案条内的 `查看详情` / `历史方案` 30px（旧规则只给 `<button>` 设了触达高度）。现在按钮与链接统一 40px，且移动端布局测试改为测量 `button` 和 `a[href]`，能真正抓到这一类回归。 |
| 200% 缩放（720x422 CSS px） | 通过。无横向溢出，8 个工具入口仍在，标题、按钮文案不截断（h1 的 `scrollHeight` 比 `clientHeight` 大 1px，是 `line-height: 1.08` 的行盒取整，`scrollWidth == clientWidth`，文字没有被裁）。 |

## 4. 修复项与未修复项

- **F1（高）移动端首屏不达标 — 已修复**（修复提交 `fix: surface toolbox on mobile fold and real
  trend omissions`）。瓶颈在工具箱之外的通用外壳：`.site-header` 202px + `#gameTabs` 290px = 492px，
  占 844px 首屏的 58%。修复方式：`web/workbench-3d.js` 在 3D 工具箱激活时给 `<body>` 加
  `three-d-toolbox-active`（离开工具箱即移除），`web/workbench-3d.css` 在 `@media (max-width: 560px)`
  内**仅在该 class 下**压缩共用外壳——站点头部隐藏装饰副标题与重复的数据行、导航改单行三列；彩种切换从
  5 个全宽按钮改为单行横向滚动（290px → 42px）。同时工具箱自身压缩标题区/状态条/磁贴，本期方案条移到
  工具网格之后（`web/analysis.html`，桌面同步生效）。首页、ssq 分析页、策略页、result/admin 的外壳
  完全没有被改动（`web/styles.css` 未改），移动端触达高度仍为 40px，数据状态条没有被隐藏。
  实测结果见 §2.1；回归测试
  `tests/test_frontend_behavior.py::test_3d_toolbox_mobile_first_screen_shows_status_and_six_tools_above_the_fold`
  会在首屏工具入口数 < 6、状态条不完整可见或磁贴低于 40px 时失败。
- **F2（中）桌面工具网格留白 — 已修复**（违反“不得出现超长空白”）。原因：`.three-d-tool-grid` 固定
  4 列，而四个分组的工具数是 4 / 2 / 1 / 1，短分组因此留下 2–3 个空单元格；网格的单元格边框由容器的
  上/左边框加磁贴的右/下边框拼出，所以这些空位是**带边框的死区**。修复方式：`web/workbench-3d.css` 把
  `.three-d-tool-grid` 从 `grid-template-columns: repeat(4, ...)` 改为 `display: flex; flex-wrap: wrap`，
  磁贴 `flex: 1 1 22%`（≤720px 时 `flex-basis: 45%`）。flex 的剩余空间按行分配，所以**每一行总是被填满**，
  任何视口下都不会再有空单元格可画；basis 把每行上限锁在桌面 4 个 / 移动端 2 个，与原来的列数一致。
  没有用占位磁贴填充，也没有只藏边框留空位。扁平磁贴、`border-radius: 0`、40px+ 触达高度均保持不变。
  实测（1440 与 390 两个视口，每组每行）：行右边缘与网格内容右边缘的差值全部为 **0px**（修复前
  号码查询差 610px、选号工具/开奖记录各差 914px）。单工具分组现在是一条整宽磁贴，不再是“一个磁贴 + 三个空格”。
- **F7（高）缩水表单卡片嵌套 — 已修复**（违反“不得出现卡片嵌套”）。原因：`缩水条件` 面板（有边框）里
  嵌 `.three-d-filter-group` 有边框的 fieldset（数值范围 / 号码形态 / 位置约束），号码形态里又嵌
  `.three-d-checks` 有边框的 fieldset（组态 / 奇数个数）——**三层可见容器**。修复方式：
  `web/workbench-3d.css` 去掉 `.three-d-filter-group` 与 `.three-d-checks` 的 `border` / `background` /
  盒内 `padding`，legend 改为普通分节标签（保留颜色与字号，加 `margin-bottom`）。**fieldset 与 legend
  元素本身全部保留**（无障碍分组不回退），只是不再画成盒子；面板成为唯一一层可见容器。实测容器嵌套
  深度从 **3**（threeDTypeGroup → threeDShapeGroup → three-d-tool-panel）降到 **1**（仅面板）。
- **F3（高）走势图的“遗漏”列恒为 0 — 已修复**。原因：`build_trend_payload` 先把当期开出的数字遗漏
  归零再写入该行，而前端 `renderTrend` 恰好渲染“当期开出数字”的遗漏（`row.omissions[position][digit]`），
  所以每行三个位置都是 `遗漏 0`。修复方式（在领域模块，不是前端糊补）：`build_trend_payload` 新增每行
  `hit_omissions[position]` —— 该位当期落号**在归零之前**已连续遗漏的期数（它结束的那段遗漏）；
  `renderTrend` 改渲染该字段。原 `omissions` 矩阵保持不变（`GET /api/3d/trends` 的既有契约、
  `tests/test_three_d_tools.py` 的断言仍适用；遗漏统计工具用的是 `/summary` 的 `position_stats`，
  不消费该字段）。走势定义文案同步说明该列语义。重新捕获的 `fc3d-toolbox-trend-desktop.png` 里，
  30 行的遗漏列是 0、1、2…9 这样真实变化的数列，不再恒为 0。
- **F4（低）禁用的主按钮仍是金色实心**：stale 下 `保存手动` 只靠 `opacity: 0.56` 变暗，一眼仍像可用的
  主 CTA。建议禁用态去掉金色填充，改为描边态。
- **F5（低）`历史方案` 在工具箱首页出现三次**：头部入口、数据状态条、本期方案条各一个。
- **F6（工程）`web/workbench-3d.js` 约 1750 行**，远超 800 行上限；本次只做局部修复，未做结构拆分。

## 5. 结论

Iteration 1 的工具箱（工具可发现性、扁平磁贴、黑金可读性、深链接、stale 降级、禁用原因就近）
在修复本轮发现的四个缺陷（图标不可见、次要文字对比度、触达目标、stale 下手动保存缺原因）后达标。
其后一次修复提交解决了两个阻塞项：移动端首屏（F1，现为状态条 + 7 个工具入口）与走势遗漏列
（F3，现为落号前的真实遗漏期数）。

再一次修复提交解决了两个视觉规则违规：工具网格的超长空白（F2）与缩水表单的卡片嵌套（F7）。
重新捕获截图后的观察：`fc3d-toolbox-home-desktop.png` 里四个分组的每一行都被磁贴填满——走势统计
仍是 4 个等宽磁贴，号码查询是 2 个半宽磁贴，选号工具 / 开奖记录各是一条整宽磁贴，边框围出的空单元格
已经消失；`fc3d-toolbox-home-mobile.png` 仍是每行 2 个磁贴（走势统计 2×2、号码查询 1 行），
选号工具为整宽磁贴，390px 无横向溢出，首屏（scrollY=0）可见数据状态条 + **7 个**工具入口；
`fc3d-toolbox-stale-mobile.png` 与 `fc3d-toolbox-reduction-mobile.png` 里缩水表单只剩面板一层边框，
数值范围 / 号码形态 / 组态 / 奇数个数 / 位置约束 都是面板内的文字分节标签，没有盒中盒，
文字无截断、无遮挡。剩余 F4 / F5 / F6 为非阻塞跟进项。
